import React, { Fragment } from 'react';

export const App = ({ storeName }) => (
    <Fragment>
        <h1>{storeName}: Delivery App</h1>
        <p>The very first store. Trust us</p>
    </Fragment>
);
