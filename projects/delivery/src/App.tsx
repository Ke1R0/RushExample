import React, { Fragment } from 'react';

export const App = ({ storeName }) => (
    <Fragment>
        <h1>{storeName}: Beta Delivery App</h1>
        <p>The very first store.</p>
    </Fragment>
);
